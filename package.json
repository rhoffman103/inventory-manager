{
  "name": "inventory-manager",
  "version": "1.0.0",
  "description": "mockup app for managing production inventory.",
  "scripts": {
    "start": "npm run start:dev",
    "start:dev": "concurrently \"npm run functions\" \"npm run client\"",
    "functions": "cd functions && npm run serve",
    "client": "cd client && yarn run start"
  },
  "author": "Bobby Hoffman",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.5.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {}
}
